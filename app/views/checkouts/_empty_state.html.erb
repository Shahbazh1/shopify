<div class="checkouts-page">

  <!-- Header -->
  <div class="page-header">
    <div class="title">
      <span class="icon">🛒</span>
      <h2>Abandoned checkouts</h2>
    </div>
  </div>

  <!-- Main Card -->
  <div class="checkout-card">

    <!-- Left Content -->
    <div class="left-content">
      <h3>Abandoned checkouts will show here</h3>

      <p>
        See when customers put an item in their cart but don’t check out.
        You can also email customers a link to their cart.
      </p>
    </div>

    <!-- Right Illustration -->
    <div class="right-content">
      <div class="circle">
        <img src="https://cdn.shopify.com/shopifycloud/web/assets/v1/vite/client/en/assets/abandoned-checkouts-empty-230c58195d01.svg" alt="Abandoned Checkouts" />
      </div>
    </div>

  </div>

  <!-- Footer Link -->
  <div class="footer-link">
    <a href="#">Learn more about abandoned checkouts</a>
  </div>

</div>

<style>
body {
  background: #f6f6f7;
  font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto;
}

/* Page */
.checkouts-page {
}

/* Header */
.page-header {
  margin-bottom: 15px;
}

.title {
  display: flex;
  align-items: center;
}

.title .icon {
  margin-right: 8px;
  font-size: 18px;
}

.title h2 {
  font-size: 20px;
  font-weight: 600;
}

/* Card */
.checkout-card {
  background: white;
  border: 1px solid #e1e3e5;
  border-radius: 16px;
  padding: 40px;

  display: flex;
  justify-content: space-between;
  align-items: center;
}

/* Left */
.left-content {
  max-width: 500px;
}

.left-content h3 {
  font-size: 22px;
  font-weight: 600;
  margin-bottom: 10px;
}

.left-content p {
  font-size: 15px;
  color: #555;
  line-height: 1.5;
}

/* Right Illustration */
.right-content {
  display: flex;
  justify-content: center;
  align-items: center;
}

.circle {
  width: 140px;
  height: 140px;
  background: #eaeaea;
  border-radius: 50%;
  position: relative;
}

/* Cart Shape */
.cart {
  width: 60px;
  height: 40px;
  border: 4px solid #1c7c54;
  border-top: none;
  position: absolute;
  bottom: 40px;
  left: 35px;
  border-radius: 4px;
}

.cart::before {
  content: "";
  position: absolute;
  width: 30px;
  height: 4px;
  background: #1c7c54;
  top: -20px;
  left: -10px;
  transform: rotate(25deg);
}

/* Wheels */
.cart::after {
  content: "";
  position: absolute;
  width: 8px;
  height: 8px;
  background: #1c7c54;
  border-radius: 50%;
  bottom: -10px;
  left: 5px;
  box-shadow: 30px 0 #1c7c54;
}

/* Cross Badge */
.cross {
  position: absolute;
  top: 10px;
  right: 10px;
  background: #e76f51;
  color: white;
  font-size: 16px;
  width: 36px;
  height: 36px;
  display: flex;
  justify-content: center;
  align-items: center;
  border-radius: 50%;
}

/* Footer */
.footer-link {
  text-align: center;
  margin-top: 30px;
}

.footer-link a {
  color: #1c1c1c;
  text-decoration: none;
  font-size: 14px;
}
</style>